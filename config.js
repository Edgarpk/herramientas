import { products as allProducts } from "./allProducts.js"; // Assuming all products are moved here

// Select a subset of products to display initially or based on some logic
// For now, let's just export all of them, but this structure allows for filtering later.
export const products = allProducts;